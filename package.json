{
	"name": "test-repo_deploy-posts-to-github-issue",
	"version": "0.0.2-beta.9",
	"scripts": {
		"build": "echo 'build'",
		"post-version": "git push origin HEAD && git push --tags"
	},
	"standard-version": {
		"scripts": {
			"posttag": "npm run post-version"
		}
	},
	"repository": {
    "type": "git",
    "url": "https://github.com/isaaxite/test-repo_deploy-posts-to-github-issue.git"
  },
	"dependencies": {
		"isubo": "0.0.11"
	},
	"devDependencies": {
		"@isubo-org/version": "0.0.2-alpha.6",
		"standard-version": "^9.5.0"
	}
}
